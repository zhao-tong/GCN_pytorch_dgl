## A implementation of GCN with PyTorch and DGL

A two layer GCN without batching for node classification.

**Example Usage**
Run with following (available dataset: "cora", "citeseer", "pubmed")
```bash
python main.py --cuda --dataset cora
```